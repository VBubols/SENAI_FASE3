const filme = { titulo: "Matrix", diretor: "Wachowskis", lancamento: 1999 };

const filmeKeys = Object.keys(filme)
const filmeCont = Object.values(filme)

console.log(filmeKeys);
console.log(filmeCont);
